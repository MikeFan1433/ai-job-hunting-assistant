# 🚀 完整部署指南 - AI Job Hunting Assistant

根据 [AI Builders 部署文档](https://space.ai-builders.com/deployments) 的完整部署流程。

## ✅ 当前状态检查

### 1. 应用准备状态 ✅

- ✅ **Dockerfile**: 已配置，符合要求
  - 使用 shell form (`sh -c`) 支持 PORT 环境变量
  - 格式：`CMD sh -c "gunicorn ... --bind 0.0.0.0:${PORT:-8000} ..."`
  
- ✅ **前端构建**: 已完成
  - `frontend/dist/` 目录存在
  - 静态文件由后端 FastAPI 服务

- ✅ **单进程架构**: 符合要求
  - FastAPI 服务所有内容（API + 静态文件）
  - 无额外进程

- ✅ **.gitignore**: 已配置
  - 排除敏感文件（.env, node_modules 等）
  - 排除构建输出和临时文件

- ✅ **Git 仓库**: 已初始化
  - 初始提交已完成
  - 所有文件已提交

### 2. 部署状态检查

运行检查脚本：
```bash
python3 check_deployment_status.py
```

**当前状态**: ⚠️ 未找到部署记录（尚未部署）

## 📋 部署所需信息

根据部署指南，需要以下**三个关键信息**：

### 1. GitHub Repository URL
- **必须是公开的仓库**（私有仓库暂不支持）
- 格式：`https://github.com/username/repo-name`
- 例如：`https://github.com/yourusername/ai-job-hunting-assistant`

### 2. Service Name
- 部署服务的唯一名称
- **将成为你的子域名**：`https://{service-name}.ai-builders.space`
- 只能包含小写字母、数字和连字符
- 例如：`ai-job-assistant` 或 `job-hunting-assistant`

### 3. Git Branch
- 要部署的 Git 分支名称
- 通常是 `main`、`master` 或 `develop`
- **必须指定**，不能为空

## 🚀 完整部署流程

### 步骤 1: 准备 GitHub 仓库

#### 1.1 在 GitHub 上创建仓库

1. 访问 https://github.com/new
2. 填写仓库信息：
   - **Repository name**: `ai-job-hunting-assistant` (或你喜欢的名称)
   - **Description**: `AI-powered job hunting assistant with resume optimization`
   - **Visibility**: 选择 **Public** (公开，必须)
   - **不要**勾选 "Initialize this repository with a README" (我们已经有了)
3. 点击 "Create repository"

#### 1.2 推送代码到 GitHub

```bash
cd "AI Job Hunting Assistant"

# 添加远程仓库（替换为你的 GitHub 用户名和仓库名）
git remote add origin https://github.com/YOUR_USERNAME/ai-job-hunting-assistant.git

# 设置主分支
git branch -M main

# 推送代码
git push -u origin main
```

**重要提示**：
- 确保仓库是**公开的**
- 确保所有更改都已提交和推送
- 部署系统会从 GitHub 拉取代码

### 步骤 2: 执行部署

#### 方法 A: 使用部署脚本（推荐）

```bash
python3 deploy_to_ai_builders.py
```

脚本会提示你输入：
1. GitHub Repository URL
2. Service Name
3. Git Branch (默认: main)

#### 方法 B: 手动调用 API

如果你已经知道所有信息，可以直接设置环境变量：

```bash
export GITHUB_REPO_URL="https://github.com/yourusername/ai-job-hunting-assistant"
export SERVICE_NAME="ai-job-assistant"
export GIT_BRANCH="main"

python3 deploy_to_ai_builders.py
```

### 步骤 3: 监控部署状态

部署通常需要 5-10 分钟。你可以：

1. **使用检查脚本**:
```bash
python3 check_deployment_status.py
```

2. **访问 Deployment Portal**:
   - 访问 https://space.ai-builders.com/deployments
   - 查看部署状态和日志

3. **检查部署日志**:
```bash
# 如果知道 service_name
python3 check_deployment_status.py
```

### 步骤 4: 验证部署

部署完成后：

1. **健康检查**:
```bash
curl https://your-service-name.ai-builders.space/api/v1/health
```

2. **访问应用**:
在浏览器中打开：
```
https://your-service-name.ai-builders.space
```

## 📝 部署脚本说明

### deploy_to_ai_builders.py

自动部署脚本，功能：
- 验证输入信息
- 调用部署 API (`POST /v1/deployments`)
- 显示部署日志
- 提供部署后的访问链接

### check_deployment_status.py

部署状态检查脚本，功能：
- 列出所有部署
- 检查特定服务的状态
- 显示部署 URL 和状态

### prepare_for_github.sh

GitHub 准备脚本，功能：
- 检查 .gitignore 配置
- 检查敏感文件
- 显示 Git 状态
- 提供下一步操作建议

## ⚠️ 重要注意事项

### 1. PORT 环境变量
- Koyeb 会在运行时设置 `PORT` 环境变量
- Dockerfile 已配置使用 `${PORT:-8000}`
- 应用代码无需修改（gunicorn 会自动使用）

### 2. AI_BUILDER_TOKEN
- 部署时自动注入，无需手动配置
- 你的应用可以通过 `os.getenv("AI_BUILDER_TOKEN")` 读取
- **不要**在 `env_vars` 中手动添加

### 3. 资源限制
- 256 MB RAM 限制
- 已优化 Dockerfile（workers 数量减少到 2）

### 4. 部署时间
- 通常需要 5-10 分钟
- 可以通过 Deployment Portal 或 API 查看状态

### 5. 代码更新
- **重要**: 所有更改必须提交并推送到 GitHub
- 推送代码**不会**自动触发重新部署
- 需要再次调用 `POST /v1/deployments` 来重新部署

## 🎯 快速部署命令

### 完整流程（一键执行）

```bash
# 1. 确保代码已提交
git add .
git commit -m "Prepare for deployment"

# 2. 推送到 GitHub（如果还没有）
git push origin main

# 3. 执行部署
python3 deploy_to_ai_builders.py
```

## 📊 部署后验证清单

- [ ] 部署状态为 "running" 或 "active"
- [ ] 健康检查端点返回 200: `/api/v1/health`
- [ ] 前端页面可以正常访问
- [ ] API 端点正常工作
- [ ] 静态资源加载正常

## 🔗 相关文件

- `deploy_to_ai_builders.py` - 部署脚本
- `check_deployment_status.py` - 状态检查脚本
- `prepare_for_github.sh` - GitHub 准备脚本
- `deploy_check.py` - 部署前检查
- `Dockerfile` - Docker 配置
- `.gitignore` - Git 忽略配置

## 🆘 故障排除

### 问题 1: 部署失败

**检查**:
1. GitHub 仓库是否是公开的
2. 仓库 URL 是否正确
3. 分支名称是否正确
4. Dockerfile 是否存在且配置正确

**查看日志**:
```bash
python3 check_deployment_status.py
```

### 问题 2: 部署状态一直显示 "deploying"

**可能原因**:
- 构建时间较长（正常）
- 构建出错（检查日志）

**解决方案**:
- 等待 10-15 分钟
- 查看构建日志
- 如果超过 20 分钟仍无进展，联系 instructor

### 问题 3: 应用无法访问

**检查**:
1. 部署状态是否为 "running"
2. 健康检查端点是否正常
3. 浏览器控制台是否有错误

## 📚 参考文档

- [AI Builders 部署文档](https://space.ai-builders.com/deployments)
- [OpenAPI 规范](https://www.ai-builders.com/resources/students-backend/openapi.json)
- `AI_BUILDERS_DEPLOYMENT_GUIDE.md` - 详细部署指南

---

**准备好部署了吗？请提供 GitHub 仓库信息，我将帮你完成部署！** 🚀
