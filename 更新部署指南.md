# 🔄 更新和部署指南

## 📋 概述

部署后，如果你修改了代码或前端设计，更新流程非常简单！只需要**两个步骤**：

1. **推送到 GitHub** - 提交并推送你的更改
2. **重新部署** - 运行部署脚本，ai-builders.space 会自动从 GitHub 拉取最新代码

## 🚀 快速更新流程

### 方法 1: 使用一键更新脚本（最简单）⭐

```bash
cd "AI Job Hunting Assistant"

# 使用默认提交信息
./update_and_deploy.sh

# 或自定义提交信息
./update_and_deploy.sh "修复前端样式问题"
```

这个脚本会自动：
1. ✅ 检查并提交所有更改
2. ✅ 推送到 GitHub
3. ✅ 询问是否重新部署
4. ✅ 执行重新部署

### 方法 2: 手动更新（两步）

#### 步骤 1: 推送到 GitHub

```bash
cd "AI Job Hunting Assistant"

# 1. 查看更改
git status

# 2. 添加所有更改
git add .

# 3. 提交更改
git commit -m "更新: 修改前端设计"

# 4. 推送到 GitHub
git push origin main
```

#### 步骤 2: 重新部署到 ai-builders.space

```bash
# 运行部署脚本（使用相同的服务名称）
python3 deploy_to_ai_builders.py
```

当提示时，输入：
- **GitHub URL**: `https://github.com/MikeFan1433/ai-job-hunting-assistant`
- **Service Name**: `ai-job-assistant` (使用之前部署时的名称)
- **Branch**: `main`

## ⚡ 更新流程详解

### ai-builders.space 的工作原理

1. **首次部署**: 
   - 从 GitHub 拉取代码
   - 使用 Dockerfile 构建 Docker 镜像
   - 部署到服务器

2. **重新部署**:
   - 再次从 GitHub 拉取最新代码
   - 重新构建 Docker 镜像
   - 更新运行中的服务

### 重要提示

- ✅ **GitHub 是唯一来源**: ai-builders.space 总是从 GitHub 拉取代码
- ✅ **自动构建**: 每次部署都会重新构建，确保使用最新代码
- ✅ **零停机更新**: 新版本部署时会平滑切换
- ⏱️ **部署时间**: 通常需要 5-10 分钟

## 📝 常见更新场景

### 场景 1: 修改前端样式

```bash
# 1. 修改前端文件（例如: frontend/src/pages/InputPage.tsx）
# 2. 提交并推送
git add .
git commit -m "更新: 优化输入页面样式"
git push origin main

# 3. 重新部署
python3 deploy_to_ai_builders.py
```

### 场景 2: 修改后端逻辑

```bash
# 1. 修改后端文件（例如: workflow_api.py）
# 2. 提交并推送
git add .
git commit -m "更新: 优化 API 响应速度"
git push origin main

# 3. 重新部署
python3 deploy_to_ai_builders.py
```

### 场景 3: 修改配置文件

```bash
# 1. 修改配置文件（例如: Dockerfile, requirements.txt）
# 2. 提交并推送
git add .
git commit -m "更新: 添加新的依赖包"
git push origin main

# 3. 重新部署
python3 deploy_to_ai_builders.py
```

## 🔧 配置自动化脚本

为了更方便，你可以设置环境变量：

```bash
# 在 ~/.zshrc 或 ~/.bashrc 中添加
export GITHUB_REPO_URL="https://github.com/MikeFan1433/ai-job-hunting-assistant.git"
export SERVICE_NAME="ai-job-assistant"
export GIT_BRANCH="main"
```

然后更新脚本会自动使用这些值：

```bash
./update_and_deploy.sh "更新描述"
```

## 📊 检查部署状态

部署后，检查状态：

```bash
# 检查部署状态
python3 check_deployment_status.py

# 或访问 Deployment Portal
# https://space.ai-builders.com/deployments
```

## ⚠️ 注意事项

1. **确保 GitHub 仓库是公开的**: ai-builders.space 只能访问公开仓库
2. **提交信息要清晰**: 使用有意义的提交信息，方便追踪更改
3. **测试本地更改**: 在推送前，确保本地测试通过
4. **环境变量**: 如果修改了 `.env` 文件，需要重新配置环境变量
5. **构建时间**: 前端构建需要时间，大更新可能需要更长时间

## 🎯 最佳实践

1. **频繁更新**: 不要积累太多更改，频繁小更新比大更新更安全
2. **使用分支**: 对于大改动，可以创建分支测试后再合并
3. **检查日志**: 部署后检查应用日志，确保没有错误
4. **备份**: 重要更改前，在 GitHub 上创建 tag 作为备份

## 💡 快速参考

```bash
# 一键更新（推荐）
./update_and_deploy.sh "更新描述"

# 手动更新
git add . && git commit -m "更新描述" && git push origin main
python3 deploy_to_ai_builders.py

# 检查状态
python3 check_deployment_status.py
```

---

**总结**: 更新流程非常简单！只需要推送到 GitHub，然后重新运行部署脚本即可。整个过程通常不超过 5 分钟！🚀
