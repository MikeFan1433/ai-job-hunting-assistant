# 🚀 本地运行指南

## ✅ 服务状态

### 后端服务
- **状态**: ✅ 运行中
- **地址**: http://localhost:8000
- **健康检查**: http://localhost:8000/api/v1/health
- **端口**: 8000

### 前端服务
- **状态**: ⏳ 正在启动
- **地址**: http://localhost:5173
- **端口**: 5173

## 📱 访问应用

在浏览器中打开：
```
http://localhost:5173
```

## 🎯 使用步骤

1. **打开浏览器** 访问 http://localhost:5173
2. **填写表单**:
   - JD (Job Description) - 必填
   - Resume - 必填（可以上传 PDF 或粘贴文本）
   - Projects - 可选
3. **点击 Start** 开始工作流
4. **等待处理** - 工作流会自动运行多个 Agent
5. **查看结果** - 完成后会跳转到 Dashboard

## 🔍 功能特性

### 1. 简历上传
- ✅ 支持 PDF 上传
- ✅ 支持拖拽上传
- ✅ 支持文本粘贴

### 2. 工作流处理
- Agent 1: 输入验证
- Agent 2: JD 分析
- Agent 3: 项目包装
- Agent 4: 简历优化
- Agent 5: 面试准备

### 3. Dashboard
- 匹配度分析
- 用户画像
- 场景分析
- 项目展示
- 优化后的简历

## 🛑 停止服务

### 方法 1: 使用终端命令

**停止后端**:
```bash
lsof -ti:8000 | xargs kill
```

**停止前端**:
```bash
lsof -ti:5173 | xargs kill
```

### 方法 2: 在运行服务的终端中
- 按 `Ctrl + C` 停止服务

## 🔧 重新启动

### 启动后端
```bash
cd "/Users/mikefan/Desktop/AI Architect - Superlinear/AI Job Hunting Assistant"
python3 -m uvicorn workflow_api:app --host 0.0.0.0 --port 8000 --reload
```

### 启动前端
```bash
cd "/Users/mikefan/Desktop/AI Architect - Superlinear/AI Job Hunting Assistant/frontend"
npm run dev
```

## 📊 服务检查

### 检查后端是否运行
```bash
curl http://localhost:8000/api/v1/health
```

应该返回：
```json
{"status":"healthy","timestamp":"..."}
```

### 检查前端是否运行
```bash
curl http://localhost:5173
```

应该返回 HTML 内容。

## 🐛 常见问题

### 问题 1: 端口被占用

**错误**: `Address already in use`

**解决方案**:
```bash
# 查看占用端口的进程
lsof -i:8000
lsof -i:5173

# 杀死进程
kill -9 <PID>
```

### 问题 2: 依赖未安装

**后端依赖**:
```bash
cd "/Users/mikefan/Desktop/AI Architect - Superlinear/AI Job Hunting Assistant"
pip3 install -r requirements.txt
```

**前端依赖**:
```bash
cd "/Users/mikefan/Desktop/AI Architect - Superlinear/AI Job Hunting Assistant/frontend"
npm install
```

### 问题 3: 前端无法连接后端

**检查**:
1. 后端是否在运行（检查端口 8000）
2. 浏览器 Console 是否有错误
3. 网络请求是否发送到正确的地址

## 💡 提示

- 后端服务支持热重载（`--reload`），修改代码后会自动重启
- 前端服务支持热重载，修改代码后会自动刷新
- 保持终端窗口打开以查看日志
- 查看浏览器 Console（F12）以查看前端日志

---

**现在可以在浏览器中访问 http://localhost:5173 查看应用了！** 🎉
